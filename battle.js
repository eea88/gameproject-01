
/* const soldiersPositionX = game.soldiers.map((eachSoldier)=>{
     return Number(eachSoldier.positionX);
}) */
/*  const soldiersPositionY = game.soldiers.map((eachSoldier)=>{
    return (eachSoldier.battleParticipants)
}) */  // Soldier Positon Y is useless as all soldiers have Positon Y inside their container, but you have the data in your array. 
/* const enemiesPositionX = game.enemies.map((eachEnemy)=>{
    return Number(eachEnemy.positionX);
})
const enemiesPositionY = game.enemies.map((eachEnemy)=>{
   return Number(eachEnemy.positionY);
})
 */

function battleParticipants(){
  
}  


function attack (){

}

function block(){

}

function receiveDamage(){

}

